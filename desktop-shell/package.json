{
  "name": "dsh-dpx-desktop-shell",
  "private": true,
  "version": "0.2.2",
  "description": "Build source for the dsh-dpx per-environment Windows desktop launcher",
  "type": "module",
  "scripts": {
    "build": "tauri build --no-bundle",
    "icon": "tauri icon app-icon.png"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.11.4"
  }
}
