{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Startup status, settings, and tray-driven windows for the desktop shell.",
  "windows": ["main", "settings", "close-dialog"],
  "permissions": ["core:default"]
}
